{% block title %}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('style/aluno.css') }}">
{% endblock %}

{% block body %}
    <h1 class="aluno-title">Cadastro de Alunos</h1>
    
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Cadastrar</button>
    {{ form_end(form) }}    

    <a href="{{ path('ver_aluno') }}" class="btn btn-secondary">Ver Alunos Cadastrados</a>

{% endblock %}